<div align="center">
  <img src="assets/blackfull.svg" alt="CF Tracker Logo" width="400"/>
  
  ---
  A web application to track Codeforces progress and contests for ICPC NMU.
</div>

## Getting Started

To easily set up and run the application on Windows:

1.  Double-click `run_app.bat`.
    *   This script will automatically create a virtual environment, install dependencies, and help you configure your `.env` file if it doesn't exist.

Alternatively, you can run it manually:

1.  Create a virtual environment: `python -m venv venv`
2.  Activate it: `venv\Scripts\activate`
3.  Install requirements: `pip install -r requirements.txt`
4.  Run the app: `python app.py`
